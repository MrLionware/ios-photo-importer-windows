<Page
    x:Class="IosPhotoImporter.App.Pages.SummaryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Import Summary" FontSize="24" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" x:Name="SummaryText" />

        <TextBlock Grid.Row="2" x:Name="DurationText" />

        <TextBlock Grid.Row="3" Text="Skipped/Failed Details" FontWeight="SemiBold" />

        <ScrollViewer Grid.Row="4" MaxHeight="260" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="DetailsText" TextWrapping="WrapWholeWords" />
        </ScrollViewer>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">
            <Button Content="Open Folder" Click="OnOpenFolderClicked" />
            <Button Content="View Logs" Click="OnViewLogsClicked" />
            <Button Content="Start Over" Click="OnRestartClicked" />
        </StackPanel>
    </Grid>
</Page>
