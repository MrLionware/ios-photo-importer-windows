<Page
    x:Class="IosPhotoImporter.App.Pages.SetupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Import Setup" FontSize="24" FontWeight="SemiBold" />

        <StackPanel Grid.Row="1" Orientation="Vertical" Spacing="8">
            <TextBlock Text="Selected Device" FontWeight="SemiBold" />
            <TextBlock x:Name="SelectedDeviceText" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Vertical" Spacing="8">
            <TextBlock Text="Destination Folder" FontWeight="SemiBold" />
            <TextBox x:Name="DestinationPathTextBox" />
        </StackPanel>

        <InfoBar
            Grid.Row="3"
            IsOpen="True"
            Severity="Informational"
            Message="Import mode is fixed to New Items Only. Existing filenames are skipped." />

        <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">
            <Button Content="Back" Click="OnBackClicked" />
            <Button Content="Start Import" Click="OnStartClicked" />
        </StackPanel>
    </Grid>
</Page>
